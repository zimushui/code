---
source: tui/src/keymap_setup.rs
expression: snapshot
---
48 columns:

  Edit Shortcut
  Open Transcript · Global
  Current ctrl-t · Default keymap
  Config `tui.keymap.global.open_transcript`
  Open the transcript overlay.

› 1. Replace binding
     Capture a replacement key for `ctrl-t`.
  2. Add alternate binding
     Keep `ctrl-t` and add another key.
  3. Replace with key chord
     Replace `ctrl-t` with a two-stroke key
     chord.
  4. Add alternate key chord
     Keep `ctrl-t` and add a two-stroke key
     chord.
  –  Remove custom binding (disabled)
     No custom root override to remove.
  5. Back to shortcuts
     Return to the shortcut list.

  Changes write the root `tui.keymap.*`
  override.
  enter select · esc back

64 columns:

  Edit Shortcut
  Open Transcript · Global
  Current ctrl-t · Default keymap
  Config `tui.keymap.global.open_transcript`
  Open the transcript overlay.

› 1. Replace binding
     Capture a replacement key for `ctrl-t`.
  2. Add alternate binding
     Keep `ctrl-t` and add another key.
  3. Replace with key chord
     Replace `ctrl-t` with a two-stroke key chord.
  4. Add alternate key chord
     Keep `ctrl-t` and add a two-stroke key chord.
  –  Remove custom binding (disabled)
     No custom root override to remove.
  5. Back to shortcuts
     Return to the shortcut list.

  Changes write the root `tui.keymap.*` override.
  enter select · esc back

96 columns:

  Edit Shortcut
  Open Transcript · Global
  Current ctrl-t · Default keymap
  Config `tui.keymap.global.open_transcript`
  Open the transcript overlay.

› 1. Replace binding                   Capture a replacement key for `ctrl-t`.
  2. Add alternate binding             Keep `ctrl-t` and add another key.
  3. Replace with key chord            Replace `ctrl-t` with a two-stroke key chord.
  4. Add alternate key chord           Keep `ctrl-t` and add a two-stroke key chord.
  –  Remove custom binding (disabled)  No custom root override to remove.
  5. Back to shortcuts                 Return to the shortcut list.

  Changes write the root `tui.keymap.*` override.
  enter select · esc back
